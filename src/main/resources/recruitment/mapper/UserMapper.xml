<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.lnu.recruitment.security.loginandauthority.login.mapper.UserDao">


    
<select id="findUser" parameterType="String" resultType="User" >
    select user_id as userid, user_name as username, user_password as password from rec_user where user_name = #{username}
</select>

<!--<insert id="registerUser" parameterType="String">-->
<!--    insert into rec_user values(#{username}, #{password})-->
<!--</insert>-->

<select id="findRole"  parameterType="Integer" resultType="Role">
    select role_name as rolename from rec_role where role_id = (select role_id from rec_user_role where user_id = #{userid})
</select>

</mapper>